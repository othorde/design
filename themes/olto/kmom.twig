<!DOCTYPE html>
<html class="no-js">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" href="{{ theme_url }}/../shared/img/favicon.ico">
    <title>{% if meta.title %}{{ meta.title }} | {% endif %}{{ site_title }}</title>

    {% if meta.description %}
        <meta name="description" content="{{ meta.description|striptags }}" />
    {% endif %}
    {% if meta.robots %}
        <meta name="robots" content="{{ meta.robots }}" />
    {% endif %}

    {% if current_page %}
        <link rel="canonical" href="{{ current_page.url }}" />
    {% endif %}

    
    {% if config.session.theme == "dark" %}
        <link rel="stylesheet" href="{{ theme_url }}/css/style-dark.css" type="text/css" />
    {% else %} 
        <link rel="stylesheet" href="{{ theme_url }}/css/style.css" type="text/css" />
    {% endif %}

</head>
<!-- "Widescreen-mode" can be activated in portfolio/config/config.yml -->
<body{% if config.theme_config.widescreen %} class="widescreen"{% endif %}>
    <header class="site-header">
        <a class="nav-toggle" title="Toggle Menu" role="button" aria-controls="nav" aria-expanded="false" tabindex="1">
            <i class="fa fa-bars" aria-hclassidden="true" class="toggler"></i>
            <span class="sr-only">Toggle Menu</span>
        </a>

        {% if pages["_meta"].meta.logo %}
        <div class="logo" aria-hidden="true">
            <a href="{{ "index"|link }}">
                <img src="{{ base_url }}/{{ pages["_meta"].meta.logo|url }}" alt="" />
            </a>
        </div>
        {% endif %}        
        <div class="nav" role="navigation" tabindex="-1">
            <ul>
                {% for page in pages(depthOffset=-1) if page.title and not page.hidden %}
                <li{% if page.class == current_page.class %} class="active" {% endif %}>
                    <a href="{{ page.url }}">{{ page.title }}</a>
                    </li>
                    {% endfor %}
            </ul>
        </div>
    </header>

    <div class="flash-region">
        <img class="flash-img" src="{{ base_url }}/image/des.png?sc=banner1&a=40,0,0,0">
    </div>
    <div class = "barofsides">
        <div class="sidebar" role="sidebar" tabindex="-1">
            <ul class = ulkmom>
                {% for page in pages("report") if page.title and not page.hidden %}
                <li{% if page.class == current_page.class %} class="active" {% endif %}>
                    <a href="{{ page.url }}">{{ page.title }}</a>
                    </li>
                    {% endfor %}
            </ul>
        </div>
        <div class="kmom">
                {{ content }}
            </div>
        </div>
    </div>


    
    <footer class="site-footer">

        <div class="social">
            <!-- Loads from FontAwesome (themes/shared/css/font-awesome.min.css) -->
            {% for social in pages["_meta"].meta.social %}
                <a href="{{ social.url }}" title="{{ social.title }}" role="button">
                    <i class="{{ social.icon }}" aria-hidden="true"></i>
                    <span class="sr-only">{{ social.title }}</span>
                </a>
            {% endfor %}
        </div>

    </footer>
    <script src="{{ theme_url }}/../shared/js/modernizr-3.3.1-custom.min.js" type="text/javascript"></script>
    <script src="{{ theme_url }}/../shared/js/utils.js" type="text/javascript"></script>
    <script src="{{ theme_url }}/../shared/js/responsive-navbar.js" type="text/javascript"></script>
</body>

</html>
